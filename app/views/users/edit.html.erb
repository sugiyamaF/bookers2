<h1>新規投稿</h1>

<%= form_with model: @user, url:'/books' do |f| %>

  <h4>タイトル</h4>
  <%= f.text_field :name %>

  <h4>本文</h4>
  <%= f.text_area :introduction %>

  <!-- 追加部分 -->
  <h4>画像</h4>
  <%= f.file_field :image, accept: "image/*" %>

  <%= f.submit '投稿' %>

<% end %>